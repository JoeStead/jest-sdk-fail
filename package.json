{
  "name": "data-scraper",
  "version": "1.0.0",
  "scripts": {
    "build": "../../node_modules/.bin/webpack --progress --color",
    "start": "../../node_modules/.bin/nodemon src/handlers/local.ts -e ts --exec ts-node",
    "test": "jest --config jest.config.ts"
  },
  "devDependencies": {
    "@types/jest": "^26.0.10",
    "ts-jest": "^26.2.0",
    "ts-loader": "^8.0.12",
    "ts-node": "^9.0.0",
    "typescript": "^4.1.3",
    "@aws-sdk/client-lambda": "^3.4.1"
  },
  "dependencies": {
  }
}
